# Button

## [Unreleased]

### Added
- `outline` / `outlineCustom` 模式（Figma `Mode=Outline` / `Outline-Custom`）：透明底 + `border-normal-primary` 外描边；主题色 / 中性色文字

## 2.1.0

### Added
- `compact` 变体（`min-w-0`）
