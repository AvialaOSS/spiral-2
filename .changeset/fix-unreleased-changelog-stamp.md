---
"@aviala-design/spiral": patch
---

Fix component changelogs that shipped as `Unreleased` in 2.6.0: stamp now accepts legacy `## Unreleased` (without brackets) and those entries are labeled `2.6.0`.
